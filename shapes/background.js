class Background {
  constructor(backgroundColor) {
    this.backgroundColor = backgroundColor;
  }

  setup() {
    background(this.backgroundColor);
  }

  draw() {
    background(this.backgroundColor);
  }
}
